// jsconfig.json tells VScode to treat this project as a JavaScript project
// By default VSCode treats it as a TypeScript project
// this file helps the auto-import

{
    "exclude": [
        "node_modules",
    ],
}